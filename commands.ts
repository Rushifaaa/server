import { Mongoose } from "mongoose";
import {run} from './server';
import { stringify } from "querystring";

const commands = (data: string, mongoose: Mongoose, mongooseConnect: string) => {

}
export default commands;